#+title: Work Plan
#+DATE: <2026-06-10 Wed 13:54>
#+AUTHOR: Mohsen BANAN
#+OPTIONS: toc:4
#+TAGS: me ai aiAndMe

# Provenance (installed once by startAiActivity.cs --- not regenerated):
#   Date:      2026-07-18T20:58:45-07:00
#   User:      bystar
#   Templates: /bisos/apps/defaults/ai-templates
#   Command:   /bisos/venv/py3/devbin/startAiActivity.cs -i initiate --root=repo --activity=bisos-pip

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: /bisos/git/auth/bxRepos/bisos-pip/capDns
  File: /bisos/git/auth/bxRepos/bisos-pip/capDns/AI-WorkPlan.org
  Activity: bisos-pip
  Companion Docs: AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org
###+END:

This Work Plan file drives the AI collaborative development.
Interactions are broken down into "Stages". Each Stage has a set
of TODOs that I work with Claude to accomplish.

Note to a fresh Claude: read this file together with AI-WORKFLOW.org,
AI-Activity.org and AI-DevStatus.org to orient yourself. The =<<Context Confirmation>>=
TODO below is the standard first step, or use =/bx-confirm0= as the automated equivalent.
TODOs tagged =:ai:= are assigned to Claude. After completing a TODO, summarize results
under =----- AI Results: ------= and mark it DONE.

* Stage 1: Confirm AI's Understanding of This Repo's Context

Goal: Confirm AI's understanding of the context and capture that in [[Context Confirmation]] TODO.


* Stage 1 TODOs
** +
** DONE [#A] <<Context Confirmation>> for AI (typically triggered by /bx-confirm0) :ai:
SCHEDULED: <2026-06-09 Tue>
CLOSED: [2026-07-18 Fri]
Based on the series of files starting from CLAUDE.md in this repo,
describe succinctly what you have understood.
Enumerate explicitly which files you have imported and which you have considered.
----- AI Results: ------
Imported: CLAUDE.md (safe-copy) → AI-WORKFLOW.org (symlink→mother/) →
AI-Activity.org (symlink→bisos-pip/) → AI-DevStatus.org (sparse, per-project) →
AI-WorkPlan.org (per-project).

This is the =bisos.capDns= pip package (bisos-pip activity), responsible for
managing DNS capability entries for BISOS services. Currently marked NOTYET /
under construction. Source at =py3/bisos/capDns/= (3 modules), seed command at
=py3/bin/capDns_seed.cs=, example =-dns.pcs= included. Conventions: CamelCase,
pathlib, type hints; developer handles all commits/pushes.
** -

* Stage 2: Convert the Starting Point Which Was Copied from bisos.gitist and make it minimally functional

Goal: A Very Basic Starting Point For Here Processing


* Stage 2 TODOs
** +
** DONE [#A] Convert all <<gitist to capDns>> --- Dont change any functionality :ai:
CLOSED: [2026-07-19 Sat]
SCHEDULED: <2026-06-09 Tue>
In py3/bin change all the imports and usages to be capDns. This will result in the seed and planted seed to be named capDns but actually function same as gitist. Based on that working starting point, we can then do needed modifications. Changes are also required in the py3/bisos/capDns directory. We wont be able to test .cs and .pcs files yet because local install of the package has not occured yet. Just do the string replacements for this TODO.
----- AI Results: ------
Replaced all gitist→capDns identifiers across py3/bin/ and py3/bisos/capDns/ (5 files).
Also updated setup.py scripts list to reflect the new filenames. No functional changes made.
** DONE [#A] <<Locally Install>> this pip pkg and run it at ./py3/test/verify.sh :me:
CLOSED: [2026-07-19 Sun 11:32] SCHEDULED: <2026-07-19 Sun 11:28>
capDns_seed.cs runs as gitist.cs so we have a running starting point.
----- AI Results: ------
pkg installed and ./py3/test/verify.sh now executes.
* DONE [#A]  Confirm for yourself that ./py3/test/verify.sh runs :ai:
CLOSED: [2026-07-19 Sat]
SCHEDULED: <2026-07-19 Sun 11:28>
Run verify.sh
----- AI Results: ------
Ran ./verify.sh from py3/test/. Both capDns_seed.cs and exmpl-here-dns.pcs execute cleanly.
capDns_seed.cs shows usage; exmpl-here-dns.pcs shows csCmnds list with capDns_* commands wired up correctly.

* DONE [#A]   Create <<needed syntax in -dns.pcs>>  ./py3/bin/airflow-here-dns.pcs to drive implementation :aiAndMe:
CLOSED: [2026-07-19 Sat]
SCHEDULED: <2026-07-19 Sun 11:28>
Define the syntax for airflow.here in ./py3/bin/airflow-here-dns.pcs so that we know how implement it in ./py3/bisos/capDns
----- AI Results: ------
airflow-here-dns.pcs syntax defined: sets cntrlInfo.dnsSpecMethod=DnsSpecMethod.etcHosts and
cntrlInfo.fqdn="airflow.here", declares 4 csCmnds (capDns_update, capDns_verify, capDns_resolve,
capDns_fqdnPing). capDns_seedInfo.py rewritten with DnsSpecMethod enum and fqdn field.
capDns_csu.py rewritten with the 4 new commands (update/verify are NOTYET stubs; resolve and
fqdnPing have real implementations). verify.sh runs cleanly.

* DONE [#A] <<Focus on .here with bisos.banna>>  Switch to bisos.banna repo for rest of the work :aiAndMe:
CLOSED: [2026-07-21 Mon]
SCHEDULED: <2026-07-19 Sun 11:28>
CapDns with dnsSpecMethod=etcHosts will use the banna machinary of pyDblock to maintain /etc/hosts.
Run it against tests/hosts at cmnd example. Point tocapDns for real /etc/hosts.
----- AI Results: ------
Work completed in =bisos-pip/banna=. See =banna/AI-WorkPlan.org= Stage 2 (all 4 TODOs DONE):
=bannaForEtcHosts= generates =/etc/hosts= entries with port comments; =updateEtcHosts= invokes
=py-dblock.cs= to expand =bx:dblock:global:run-result-stdout= blocks in a target file.
Output format: =127.0.22.1xx  <name>.here  # port=NNNNN=, sorted alphabetically from 127.0.22.100.
Both repo and site-packages are in sync.

* DONE [#A] <<After bisos.banna, complete this capDns >>  Switch back to here (capDns) and get ping airflow.here to works  :aiAndMe:
CLOSED: [2026-07-21 Mon]
SCHEDULED: <2026-07-19 Sun 11:28>
Get to the point where airflow.here is in /etc/hosts and that we can ping it through airflow-dns.pcs.
The capDns_verify should verify that /etc/hosts has the dblock BEGIN END for the banna.
In all examples of capDns_csu.py delete the # doContinue comment, it is loft over that do no belong there.
In capDns_update Cmnd sudo run cmnd of bisos.banna for updating of /etc/hosts.
Before running the dblock update, safe copy to /etc/hosts.dateTag.
For capDns_resolve , give it and arg and run getent ahosts airflow.here
Get rid of csCmndsRun  and csCmndEval and their  examples. They are not applicable here.
----- AI Results: ------
Implemented all changes in =capDns_csu.py=:
- =capDns_update=: safe-copies =/etc/hosts= to =/etc/hosts.<dateTag>=, then runs
  =sudo bannaInfo.cs -i updateEtcHosts /etc/hosts=.
- =capDns_verify=: reads =/etc/hosts= and checks for both =####+BEGIN:= and =####+END:= markers.
- =capDns_resolve=: now takes optional fqdn arg, runs =getent ahosts <fqdn>=.
- =examples_csu=: replaced stale placeholder with real =capDns_*= examples; no =# doContinue= comments.
Cleaned up =airflow-here-dns.pcs= and =exmpl-here-dns.pcs=: removed all =doContinue=True= and
removed the stale =examples_pcs= function. All 4 commands show =doContinue=False= in the .pcs output.

** -


# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# eval: (setq-local org-log-done 'time)
# End:
